# Sui Move
build/
Move.lock

# Foundry / EVM
cache/
out/
broadcast/

# Rust / Solana / Anchor
target/
.anchor/
test-ledger/
*.so

# Node
node_modules/
npm-debug.log
yarn-error.log

# Editors / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Local secrets — .env files
.env
.env.*
!.env.example

# Wallet / keypair files (Solana, Foundry, Sui, generic)
# Repo is public — any local-exported key here would be game over.
id.json
keypair.json
*.keypair
*.keystore
*.key
*.pem
keypairs/
wallets/
sui.keystore
*.alias

# Test / coverage / gas reports (may leak local RPC URLs in headers)
coverage/
coverage.json
lcov.info
gas-report*.txt

# claude code preview harness (local-only)
.claude/

# Local recovery snapshots
.backup/

# Generic transient
*.tmp
*.bak
~$*
Thumbs.db
Desktop.ini
